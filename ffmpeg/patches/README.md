# Native patches

No FFmpeg source patches are required for core 0.1.0. The deliberately reduced ffmpeg.wasm binding and link recipe live under `scripts/native/` so they are included directly in corresponding source archives.
